randomize.addEventListener('click', randomItem);

function randomItem() {
var random=list.value.split(',', '\n');
) 
  
  
  
 display.innerHTML = random[Math.floor(Math.random() * random.length)];
  
}
